"""
Task 1: Input Tracker & Validation
📌 Goal: Collect and validate the user's meal and workout details

✅ What to do:
1. Define a function `get_non_empty_input(prompt)` that:
   - Uses a while loop to prompt until the user enters something non-blank.
   - Returns the valid string.

2. Define a function `get_daily_log()` that:
   - Greets the user.
   - Asks for three meals and their calorie values.
   - Asks for two workouts and their calorie burns.
   - Use `get_non_empty_input()` for all prompts.
   - Accumulate calories using variables (not lists).
   - Return `total_intake` and `total_burn`.

# Example placeholder function signatures:
# def get_non_empty_input(prompt):
#     # TODO: Implement loop to ensure input is not empty

# def get_daily_log():
#     # TODO: Use get_non_empty_input to prompt for 3 meals and 2 workouts
"""
