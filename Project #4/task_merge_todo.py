"""
Task Merge: Integration Guide
🤝 Goal: Combine all individual tasks into a smooth, functional program

✅ What to do:
1. Ensure all four functions (and helper function) exist:
   - get_non_empty_input(prompt)
   - get_daily_log()
   - calculate_net_calories(intake, burn)
   - generate_feedback(net_calories)
   - print_daily_report()

2. Confirm that each function returns expected values:
   - Intake and burn from get_daily_log()
   - Net and message from calculate_net_calories()
   - Feedback from generate_feedback()

3. Use `print_daily_report()` as the main entry point for the program.

4. Run a final test using different input values:
   - Try edge cases like high intake, low burn, and vice versa

5. Ensure feedback logic works and output formatting looks clean.

6. Clean up unused variables, add comments, and make the program user-friendly.

# Final section (main trigger):
# if __name__ == "__main__":
#     print_daily_report()
"""
